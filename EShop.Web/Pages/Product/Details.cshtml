@page
@model EShop.Web.Pages.Product.DetailsModel
@{
    ViewData["Title"] = "جزئیات محصول";
}

<div class="card shadow-sm p-4">

    <div class="row">

        <div class="col-md-5 text-center">
            <img src="@Model.Product.ImagePath"
                 class="img-fluid rounded"
                 style="max-height:350px; object-fit:cover;" />
        </div>

        <div class="col-md-7">

            <h3 class="mb-3">@Model.Product.Title</h3>

            <p class="text-muted fs-5">
                قیمت: @Model.Product.UnitCost.ToString("N0") تومان
            </p>

            <p>
                <strong>موجودی:</strong> @Model.Product.Stock عدد
            </p>

            <p>
                <strong>امتیاز:</strong>
                @for (int i = 1; i <= 5; i++)
                {
                    if (i <= Model.Product.Rate)
                    {
                        <span class="text-warning">&#9733;</span>
                    }
                    else
                    {
                        <span class="text-secondary">&#9733;</span>
                    }
                }
            </p>

            <div class="mt-4 d-flex gap-2">

                <button class="btn btn-outline-primary add-to-card" data-id="@Model.Product.Id"
                        data-title="@Model.Product.Title"
                        data-price="@Model.Product.UnitCost">
                    <i class="fa fa-shopping-cart"></i> سبد
                </button>

                <a asp-page="/Index" class="btn btn-secondary">بازگشت</a>

            </div>

        </div>

    </div>

</div>
@section Scripts{
    <script src="~/js/shopcart.js" defer></script>
@*     <script src="~/js/addToBasket.js"></script>
 *@
}